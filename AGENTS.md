> 本项目的过程治理以根目录 `PDF题库项目启动总控说明_V3.1.md` 为准，开工前必须读完。

## 已定决策台账 (从总控说明同步)
| 决策项 | 结论 | 确认日期 |
| --- | --- | --- |
| 语言与技术栈 | Python 3.11 + PySide6 + PyInstaller | 2026-08-11 |
| 目标平台 | 仅 Windows 10 1809 x64，需 AVX2 | 2026-08-11 |
| 模型交付 | 单独模型包，手动放入 `models/`；缺失可进受限模式 | 2026-08-11 |
| **文档来源** | **原生与扫描混合，扫描页约 50%，按页判定** | 2026-08-11 |
| **版式范围** | **本期只做单栏；疑似双栏报 WARNING 转 Review，禁止硬跑** | 2026-08-11 |
| **题型构成** | **文字 : 表格 : 图形 = 4 : 2 : 4，即 60% 含非文本元素** | 2026-08-11 |
| 使用者模型 | 单人单机，无账号与权限 | 2026-08-11 |
| 人工修图 | 不做；图像处理必须是可重放参数 | 2026-08-11 |
| 跨任务去重 | 不做 | 2026-08-11 |
| **分发与许可证** | **本版仅自用、不分发 → 采用 PyMuPDF。完全封在 `PDFBackend` 后。** | 2026-08-11 |
| 首版交付范围 | 完整交付：OCR + PySide6 界面 + 分档指标 + 打包；单 Goal 执行 | 2026-08-11 |

---

# 项目开发通用规则

你是本项目的 AI 开发助手。所有开发工作必须遵守本文规则。

## 1. 工作边界

所有代码、文档、配置和资源文件都必须创建在当前项目根目录内。

禁止在项目外部创建文件。
禁止在桌面、下载目录、用户根目录或其他无关目录创建项目文件。
禁止额外套一层同名项目文件夹，除非用户明确要求。

如果当前目录是空项目，先创建基础文档和项目结构。
如果当前目录已经有代码，先读取现有目录结构，再基于现有结构继续开发，不要重建项目。

## 2. Spec First 开发原则

本项目采用 Spec-Driven Development。

在需求、规格、技术设计和任务拆分没有完成并得到用户确认前，不要直接编写业务代码。

标准顺序如下：

1. 需求文档 Requirements
2. 功能规格 Functional Spec
3. 技术设计 Technical Design
4. 任务拆分 Task Breakdown
5. 编码实现 Implementation
6. 测试验证 Testing
7. 交付记录 Delivery Notes

没有 Spec，不写代码。
Spec 变化，先更新文档，再修改代码。
所有代码实现必须能对应到具体需求和任务编号。

## 3. 必须维护的文件

项目根目录必须包含：

```text
AGENTS.md
SPEC.md
README.md
docs/
  01-requirements.md
  02-functional-spec.md
  03-technical-design.md
  04-tasks.md
  05-test-plan.md
  06-delivery-notes.md
```

文件用途：

- `AGENTS.md`：AI 开发规则
- `SPEC.md`：项目总规格入口
- `README.md`：面向人的项目说明
- `docs/01-requirements.md`：需求文档
- `docs/02-functional-spec.md`：功能规格
- `docs/03-technical-design.md`：技术设计
- `docs/04-tasks.md`：任务拆分
- `docs/05-test-plan.md`：测试计划
- `docs/06-delivery-notes.md`：交付记录

## 4. 需求阶段规则

开始编码前，必须先明确：

- 项目背景
- 项目目标
- 目标用户
- 核心场景
- MVP 范围
- 暂不开发内容
- 页面或功能列表
- 用户流程
- 验收标准

如果需求不清楚，先提出简短问题确认。
不要自行扩大项目范围。
不要把“以后可能需要”的功能提前实现。

## 5. 功能规格规则

每个功能都必须写清楚：

- 功能目标
- 输入内容
- 输出结果
- 正常流程
- 异常流程
- 空状态
- 加载状态
- 错误提示
- 权限和边界条件
- 验收条件

不要只描述界面，要描述真实业务行为。

## 6. 技术设计规则

技术设计必须包含：

- 技术栈选择
- 项目目录结构
- 核心模块划分
- 数据模型
- API 或数据交互方式
- 状态管理方案
- 错误处理方案
- 测试方案
- 启动和部署方式

优先沿用项目已有技术栈、代码风格和目录结构。
不要随便引入新框架或新依赖。
如果确实需要新增依赖，必须先说明原因、用途和影响。

## 7. 任务拆分规则

任务必须足够小，可以独立实现和验证。

每个任务必须包含：

- 任务编号
- 任务目标
- 涉及文件
- 实现内容
- 验收标准
- 验证方式
- 当前状态

任务状态只能使用：

- `未开始`
- `进行中`
- `已完成`
- `已阻塞`

优先顺序：

1. 项目基础结构
2. 核心数据模型
3. 核心业务闭环
4. 页面和交互
5. 异常、空状态、加载状态
6. 测试和验证
7. 优化和部署

## 8. 编码规则

每次只实现一个明确任务。

修改前必须说明：

- 本次要完成什么
- 准备修改哪些文件
- 为什么需要这些修改

修改时必须遵守：

- 只修改和当前任务相关的文件
- 不做无关重构
- 不删除已有功能，除非用户明确要求
- 不重写整个项目，除非用户明确要求
- 不把临时代码混入正式逻辑
- 不制造无法运行的半成品
- 新增代码要清晰、简单、可维护

如果发现已有代码问题：

1. 先说明问题
2. 说明影响范围
3. 再决定是否在当前任务中修复

## 9. 前端规则

优先实现真实可用的产品界面，不要先做空洞宣传页，除非项目目标就是官网。

页面必须考虑：

- 正常状态
- 空状态
- 加载状态
- 错误状态
- 表单校验
- 操作反馈
- 移动端适配
- 桌面端适配

界面要简洁、清晰、适合真实用户使用。
避免文字溢出、元素重叠、布局错乱。

## 10. 后端和数据规则

先设计数据模型，再实现接口或数据读写。

接口和数据结构必须稳定、清晰、可追踪。

必须考虑：

- 创建
- 查询
- 更新
- 删除
- 校验
- 错误处理
- 权限边界

不要把测试假数据伪装成正式逻辑。
如果使用 mock 数据，必须明确标注。

## 11. 测试验证规则

每个任务完成后必须尽可能验证：

- 项目能否启动
- 是否有语法错误
- 是否有类型错误
- 核心流程是否跑通
- 页面是否正常显示
- 表单和按钮是否可用
- 接口或数据读写是否正确
- 是否破坏已有功能

如果无法验证，必须说明原因。

## 12. 文档同步规则

开发过程中如果发生以下变化，必须同步更新文档：

- 需求变化
- 功能范围变化
- 技术方案变化
- 数据结构变化
- API 变化
- 任务状态变化
- 验证结果变化

不要让代码和 Spec 脱节。

## 13. 交付回复格式

每次完成后，必须用以下格式回复：

```text
当前阶段：
本次完成：
修改文件：
验证结果：
风险或未完成事项：
下一步建议：
```

回复要具体，不要只说“已完成”。

## 14. 默认项目初始化要求

当用户要求初始化项目时，先创建或确认以下结构：

```text
AGENTS.md
SPEC.md
README.md
docs/
  01-requirements.md
  02-functional-spec.md
  03-technical-design.md
  04-tasks.md
  05-test-plan.md
  06-delivery-notes.md
```

然后根据项目类型再创建代码目录。

前端项目可使用：

```text
src/
public/
tests/
```

后端项目可使用：

```text
src/
tests/
scripts/
config/
```

全栈项目可使用：

```text
frontend/
backend/
docs/
tests/
```

不要机械创建无用目录。目录结构必须服务于项目实际需求。
